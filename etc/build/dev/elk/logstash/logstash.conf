input {
  file {
    type => "nginx_access"
    path => "/app/var/logs/nginx/access.log"
    start_position => beginning
  }
  file {
    type => "sharck_dev"
    path => "/app/var/logs/dev.log"
    start_position => beginning
  }
  file {
    type => "sharck_prod"
    path => "/app/var/logs/prod.log"
    start_position => beginning
  }
}

filter {
  if [type] == "nginx_access" {
    grok {
      patterns_dir => "./patterns"
      match => { "message" => "%{NGINXACCESS}"}
    }
  }
  else if [type] in ["sharck_dev", "sharck_prod"] {
    grok {
      patterns_dir => "./patterns"
      match => { "message" => "%{SYMFONY}"}
    }
  }
}

output {
  elasticsearch {
    host => "localhost"
    cluster => "logstash"
  }
}